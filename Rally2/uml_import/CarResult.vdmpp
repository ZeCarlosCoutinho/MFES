class CarResult
instance variables
  public event:Event;
  public car:Car;
  public position:int;

operations
  public  CarResult : Car * Event * int ==> CarResult
  CarResult(cr, evnt, pos) ==
    (
    event := evnt;
    car := cr;
    position := pos;
    );


end CarResult
